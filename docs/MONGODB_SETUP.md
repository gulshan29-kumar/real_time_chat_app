# MongoDB Atlas Setup Guide

1. Create a free cluster at [cloud.mongodb.com](https://cloud.mongodb.com).
2. Configure Network Access to allow `0.0.0.0/0`.
3. Copy connection string to `MONGODB_URI` in `server/.env`.
