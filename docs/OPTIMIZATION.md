# Performance Optimization Notes

- **Vite Chunk Splitting**: Vendor chunks are bundled separately for optimal browser caching.
- **Tailwind CSS v4**: Zero unused CSS in production bundle.
- **Socket.IO Rooms**: Efficient event multiplexing over long polling fallbacks.
